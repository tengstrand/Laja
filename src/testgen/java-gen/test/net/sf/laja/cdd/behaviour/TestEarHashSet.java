package net.sf.laja.cdd.behaviour;

import net.sf.laja.cdd.state.ear.*;
import net.sf.laja.cdd.behaviour.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class TestEarHashSet implements TestEarSet, RandomAccess, Cloneable, java.io.Serializable {
    protected EarStateSet stateSet;
    protected final Set<TestEar> set;

    public TestEarHashSet(TestEar... array) {
        this.set = new HashSet<TestEar>();
        this.set.addAll(Arrays.asList(array));
    }

    public TestEarHashSet(Collection<TestEar> collection) {
        this.set = new HashSet<TestEar>();
        this.set.addAll(collection);
    }

    public TestEarHashSet(EarStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<TestEar> elements = new HashSet<TestEar>(stateSet.size());

        for (EarState state : stateSet) {
            EarStateBuilder builder = new EarStateBuilderImpl(state);
            TestEar entry = (TestEar) builder.as(new TestEarFactory.TestEarFactory_());
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public class StateInSyncSet extends HashSet<TestEar> {
        private EarStateSet stateSet;

        public StateInSyncSet(EarStateSet stateSet, Set<TestEar> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(TestEar element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestEarHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends TestEar> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestEarHashSet.this);
            boolean modified = super.addAll(collection);

            for (TestEar element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestEarHashSet.this);
            if (!(element instanceof TestEar)) {
                return false;
            }
            boolean removedState = stateSet.remove(((TestEar) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestEarHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof TestEar) {
                    elements.add(element);
                    states.add(((TestEar)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestEarHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof TestEar) {
                    elements.add(element);
                    states.add(((TestEar)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestEarHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<TestEar> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <TestEar> TestEar[] toArray(TestEar[] array) {
        return set.toArray(array);
    }

    public boolean add(TestEar element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends TestEar> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof TestEar)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
