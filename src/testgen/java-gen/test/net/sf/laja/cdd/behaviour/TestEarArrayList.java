package net.sf.laja.cdd.behaviour;

import net.sf.laja.cdd.state.ear.*;
import net.sf.laja.cdd.behaviour.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class TestEarArrayList implements TestEarList, RandomAccess, Cloneable, java.io.Serializable {
    protected EarStateList stateList;
    protected final List<TestEar> list;

    public TestEarArrayList(TestEar... array) {
        this.list = new ArrayList<TestEar>();
        this.list.addAll(Arrays.asList(array));
    }

    public TestEarArrayList(Collection<TestEar> collection) {
        this.list = new ArrayList<TestEar>();
        this.list.addAll(collection);
    }

    public TestEarArrayList(EarStateList stateList) {
        this.stateList = stateList;
        this.stateList.encapsulate(this);
        List<TestEar> elements = new ArrayList<TestEar>(stateList.size());

        for (EarState state : stateList) {
            EarStateBuilder builder = new EarStateBuilderImpl(state);
            TestEar entry = (TestEar) builder.as(new TestEarFactory.TestEarFactory_(builder));
            elements.add(entry);
        }
        this.list = new StateInSyncList(stateList, elements);
    }

    public class StateInSyncList extends ArrayList<TestEar> {
        private final EarStateList stateList;

        public StateInSyncList(EarStateList stateList, List<TestEar> elements) {
            this.stateList = stateList;
            super.addAll(elements);
        }

        @Override
        public boolean add(TestEar element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            stateList.add(element.getState(stateList.certificate()));
            return super.add(element);
        }

        @Override
        public void add(int index, TestEar element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            stateList.add(index, element.getState(stateList.certificate()));
            super.add(index, element);
        }

        @Override
        public boolean addAll(Collection<? extends TestEar> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            boolean modified = super.addAll(collection);

            for (TestEar element : collection) {
                stateList.add(element.getState(stateList.certificate()));
            }
            return modified;
        }

        @Override
        public boolean addAll(int index, Collection<? extends TestEar> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            boolean modified = super.addAll(index, collection);

            List elements = new ArrayList(collection.size());
            for (TestEar element : collection) {
                elements.add(element.getState(stateList.certificate()));
            }
            stateList.addAll(index, elements);

            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            if (!(element instanceof TestEar)) {
                return false;
            }
            stateList.remove(((TestEar) element).getState(stateList.certificate()));

            return super.remove(element);
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            List states = new ArrayList(collection.size());
            List elements = new ArrayList(collection.size());
            for (Object element : collection) {
                if (element instanceof TestEar) {
                    elements.add(element);
                    states.add(((TestEar)element).getState(stateList.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateList.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            List states = new ArrayList(collection.size());
            List elements = new ArrayList(collection.size());
            for (Object element : collection) {
                if (element instanceof TestEar) {
                    elements.add(element);
                    states.add(((TestEar)element).getState(stateList.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateList.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            stateList.clear();
            super.clear();
        }

        @Override
        public TestEar set(int index, TestEar element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            stateList.set(index, element.getState(stateList.certificate()));
            return super.set(index, element);
        }

        @Override
        public TestEar remove(int index) {
            stateList.throwExceptionIfNotEncapsulatedBy(TestEarArrayList.this);
            stateList.remove(index);
            return super.remove(index);
        }
    }

    public int size() {
        return list.size();
    }

    public boolean isEmpty() {
        return list.isEmpty();
    }

    public boolean contains(Object element) {
        return list.contains(element);
    }

    public Iterator<TestEar> iterator() {
        return list.iterator();
    }

    public Object[] toArray() {
        return list.toArray();
    }

    public <TestEar> TestEar[] toArray(TestEar[] array) {
        return list.toArray(array);
    }

    public boolean add(TestEar element) {
        return list.add(element);
    }

    public void add(int index, TestEar element) {
        list.add(index, element);
    }

    public boolean addAll(Collection<? extends TestEar> collection) {
        return list.addAll(collection);
    }

    public boolean addAll(int index, Collection<? extends TestEar> collection) {
        return list.addAll(index, collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof TestEar)) {
            return false;
        }
        return list.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return list.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return list.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return list.retainAll(collection);
    }

    public void clear() {
        list.clear();
    }

    public TestEar get(int index) {
        return list.get(index);
    }

    public TestEar set(int index, TestEar element) {
        return list.set(index, element);
    }

    public TestEar remove(int index) {
        return list.remove(index);
    }

    public int indexOf(Object element) {
        return list.indexOf(element);
    }

    public int lastIndexOf(Object element) {
        return list.lastIndexOf(element);
    }

    public ListIterator<TestEar> listIterator() {
        return list.listIterator();
    }

    public ListIterator<TestEar> listIterator(int index) {
        return list.listIterator(index);
    }

    public List<TestEar> subList(int fromIndex, int toIndex) {
        return list.subList(fromIndex, toIndex);
    }

    @Override
    public boolean equals(Object o) {
        return list.equals(o);
    }

    @Override
    public int hashCode() {
        return list.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + list + '}';
    }
}
