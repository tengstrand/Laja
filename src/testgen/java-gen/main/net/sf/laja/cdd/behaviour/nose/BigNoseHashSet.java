package net.sf.laja.cdd.behaviour.nose;

import net.sf.laja.cdd.state.nose.*;
import net.sf.laja.cdd.behaviour.nose.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class BigNoseHashSet implements BigNoseSet, RandomAccess, Cloneable, java.io.Serializable {
    protected NoseStateSet stateSet;
    protected final Set<BigNose> set;

    public BigNoseHashSet(BigNose... array) {
        this.set = new HashSet<BigNose>();
        this.set.addAll(Arrays.asList(array));
    }

    public BigNoseHashSet(Collection<BigNose> collection) {
        this.set = new HashSet<BigNose>();
        this.set.addAll(collection);
    }

    public BigNoseHashSet(NoseStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<BigNose> elements = new HashSet<BigNose>(stateSet.size());

        for (NoseState state : stateSet) {
            NoseStateBuilder builder = new NoseStateBuilderImpl(state);
            BigNose entry = (BigNose) builder.as(new NoseFactory.BigNoseFactory_());
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public class StateInSyncSet extends HashSet<BigNose> {
        private NoseStateSet stateSet;

        public StateInSyncSet(NoseStateSet stateSet, Set<BigNose> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(BigNose element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BigNoseHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends BigNose> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BigNoseHashSet.this);
            boolean modified = super.addAll(collection);

            for (BigNose element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BigNoseHashSet.this);
            if (!(element instanceof BigNose)) {
                return false;
            }
            boolean removedState = stateSet.remove(((BigNose) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BigNoseHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof BigNose) {
                    elements.add(element);
                    states.add(((BigNose)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BigNoseHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof BigNose) {
                    elements.add(element);
                    states.add(((BigNose)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(BigNoseHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<BigNose> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <BigNose> BigNose[] toArray(BigNose[] array) {
        return set.toArray(array);
    }

    public boolean add(BigNose element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends BigNose> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof BigNose)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
