package net.sf.laja.cdd.state.toe;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class ToeStateListBuilder {
    private final Certificate certificate;
    private final ToeStateList states;

    public ToeStateListBuilder() {
        certificate = Certificate.get(new ToeStateImpl());
        states = ToeStateListImpl.emptyList();
    }

    public ToeStateListBuilder(ToeStateList states) {
        this.states = states;
        certificate = Certificate.get(new ToeStateImpl());
    }

    public ToeStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<ToeStateBuilder> getStateBuilders() {
        List<ToeStateBuilder> builders = new ArrayList<ToeStateBuilder>(states.size());

        for (ToeState state : states) {
            builders.add(new ToeStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(ToeStateBuilder builder) {
        states.add(builder.getToeState(certificate));
    }

    public void add(ToeState state) {
        states.add(state);
    }

    public void add(int index, ToeState state) {
        states.add(index, state);
    }

    public void addAll(int index, ToeStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, ToeState state) {
        states.set(index, state);
    }

    public boolean remove(ToeState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(ToeStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "ToeStateListBuilder{" + states + "}";
    }
}
