package net.sf.laja.cdd.state.nail;

import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class NailStateBuilderImpl implements NailStateBuilder {
    private Object encapsulator;
    private NailState state;
    private final Certificate certificate;

    NailStateBuilderImpl() {
        state = new NailStateImpl();
        certificate = Certificate.get(this);
    }

    public NailStateBuilderImpl(NailState state) {
        this.state = state;
        certificate = Certificate.get(this);
    }

    public NailStateBuilderImpl(NailState state, Object encapsulator) {
        this(state);
        this.encapsulator = encapsulator;
    }

    public void withColor(String color) {
        state.setColor(color, encapsulator);
    }

    public boolean isValid() {
        return state.isValid();
    }

    public Object as(NailBehaviourFactory factory, Object... args) {
        return factory.create(state, args);
    }

    public NailState getNailState(net.sf.laja.cdd.state.Certificate certificate) {
        return state;
    }

    @Override
    public int hashCode() {
        return state.hashCode();
    }
    @Override
    public boolean equals(Object obj) {
        if (!(obj instanceof NailStateComparable)) return false;

        return true;
    }

    @Override
    public String toString() {
        return state.toString();
    }
}
