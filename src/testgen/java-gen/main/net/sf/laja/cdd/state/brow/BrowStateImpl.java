package net.sf.laja.cdd.state.brow;

import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class BrowStateImpl implements BrowState {
    protected int area;

    private boolean _encapsulated = false;

    BrowStateImpl() {
    }

    public BrowStateImpl(Certificate certificate) {
        this();
        if (certificate == null) {
            throw new IllegalArgumentException("Certificate can not be null");
        }
    }

    public static BrowStateBuilder build() {
        return new BrowStateBuilderImpl();
    }

    public static BrowStateBuilder build(BrowState state) {
        return new BrowStateBuilderImpl(state);
    }

    public Certificate certificate() {
        return Certificate.get(this);
    }

    public boolean isValid() {
        return isValid(_encapsulated);
    }

    public boolean isValidAsEncapsulated() {
        return isValid(true);
    }

    private boolean isValid(boolean encapsulated) {
        return true;
    }

    // Getters
    public int getArea() { return area; }

    // Setters
    public void setArea(int area) { this.area = area; }

    public void encapsulate() {
        _encapsulated = true;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        BrowStateImpl state = (BrowStateImpl)o;

        if (area != state.getArea()) return false;

        return true;
    }

    public boolean valueEquals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        BrowStateImpl state = (BrowStateImpl)o;

        if (area != state.getArea()) return false;

        return true;
    }

    @Override
    public int hashCode() {
        int result = area;

        return result;
    }

    public int valueHashCode() {
        int result = area;

        return result;
    }

    @Override
    public String toString() {
        return "{area=" + area + "}";
    }
}
