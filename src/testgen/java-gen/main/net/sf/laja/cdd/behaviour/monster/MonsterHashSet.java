package net.sf.laja.cdd.behaviour.monster;

import net.sf.laja.cdd.state.monster.*;
import net.sf.laja.cdd.behaviour.monster.*;

import net.sf.laja.cdd.behaviour.terrestrial.Terrestrial;
import net.sf.laja.cdd.behaviour.terrestrial.TerrestrialSet;
import net.sf.laja.cdd.behaviour.terrestrial.TerrestrialHashSet;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class MonsterHashSet implements MonsterSet, RandomAccess, Cloneable, java.io.Serializable {
    protected MonsterStateSet stateSet;
    protected final Set<Monster> set;

    public MonsterHashSet(Monster... array) {
        this.set = new HashSet<Monster>();
        this.set.addAll(Arrays.asList(array));
    }

    public MonsterHashSet(Collection<Monster> collection) {
        this.set = new HashSet<Monster>();
        this.set.addAll(collection);
    }

    public MonsterHashSet(MonsterStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<Monster> elements = new HashSet<Monster>(stateSet.size());

        for (MonsterState state : stateSet) {
            MonsterStateBuilder builder = new MonsterStateBuilderImpl(state);
            Monster entry = (Monster) builder.as(new MonsterFactory.MonsterFactory_(builder));
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public TerrestrialHashSet asTerrestrialSet() {
        Set<Terrestrial> result = new HashSet<Terrestrial>();
        for (Monster entry : set) {
            result.add(entry.asTerrestrial());
        }
        return new TerrestrialHashSet(result);
    }

    public class StateInSyncSet extends HashSet<Monster> {
        private MonsterStateSet stateSet;

        public StateInSyncSet(MonsterStateSet stateSet, Set<Monster> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(Monster element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(MonsterHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends Monster> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(MonsterHashSet.this);
            boolean modified = super.addAll(collection);

            for (Monster element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(MonsterHashSet.this);
            if (!(element instanceof Monster)) {
                return false;
            }
            boolean removedState = stateSet.remove(((Monster) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(MonsterHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Monster) {
                    elements.add(element);
                    states.add(((Monster)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(MonsterHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Monster) {
                    elements.add(element);
                    states.add(((Monster)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(MonsterHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<Monster> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <Monster> Monster[] toArray(Monster[] array) {
        return set.toArray(array);
    }

    public boolean add(Monster element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends Monster> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Monster)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
