package net.sf.laja.cdd.state.mouth;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public final class MouthStateListBuilder {
    private final Certificate certificate;
    private final MouthStateList states;

    public MouthStateListBuilder() {
        certificate = Certificate.get(new MouthStateImpl());
        states = MouthStateListImpl.emptyList();
    }

    public MouthStateListBuilder(MouthStateList states) {
        this.states = states;
        certificate = Certificate.get(new MouthStateImpl());
    }

    public MouthStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<MouthStateBuilder> getStateBuilders() {
        List<MouthStateBuilder> builders = new ArrayList<MouthStateBuilder>(states.size());

        for (MouthState state : states) {
            builders.add(new MouthStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(MouthStateBuilder builder) {
        states.add(builder.getMouthState(certificate));
    }

    public void add(MouthState state) {
        states.add(state);
    }

    public void add(int index, MouthState state) {
        states.add(index, state);
    }

    public void addAll(int index, MouthStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, MouthState state) {
        states.set(index, state);
    }

    public boolean remove(MouthState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(MouthStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "MouthStateListBuilder{" + states + "}";
    }
}
