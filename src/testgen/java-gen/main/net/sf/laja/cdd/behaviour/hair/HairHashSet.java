package net.sf.laja.cdd.behaviour.hair;

import net.sf.laja.cdd.state.hair.*;
import net.sf.laja.cdd.behaviour.hair.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class HairHashSet implements HairSet, RandomAccess, Cloneable, java.io.Serializable {
    protected HairStateSet stateSet;
    protected final Set<Hair> set;

    public HairHashSet(Hair... array) {
        this.set = new HashSet<Hair>();
        this.set.addAll(Arrays.asList(array));
    }

    public HairHashSet(Collection<Hair> collection) {
        this.set = new HashSet<Hair>();
        this.set.addAll(collection);
    }

    public HairHashSet(HairStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<Hair> elements = new HashSet<Hair>(stateSet.size());

        for (HairState state : stateSet) {
            HairStateBuilder builder = new HairStateBuilderImpl(state);
            Hair entry = (Hair) builder.as(new HairFactory.HairFactory_());
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public FakeHairHashSet asFakeHairSet() {
        if (stateSet != null) {
            return new FakeHairHashSet(stateSet);
        }
        Set<FakeHair> result = new HashSet<FakeHair>();
        for (Hair entry : set) {
            result.add(entry.asFakeHair());
        }
        return new FakeHairHashSet(result);
    }

    public class StateInSyncSet extends HashSet<Hair> {
        private HairStateSet stateSet;

        public StateInSyncSet(HairStateSet stateSet, Set<Hair> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(Hair element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(HairHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends Hair> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(HairHashSet.this);
            boolean modified = super.addAll(collection);

            for (Hair element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(HairHashSet.this);
            if (!(element instanceof Hair)) {
                return false;
            }
            boolean removedState = stateSet.remove(((Hair) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(HairHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Hair) {
                    elements.add(element);
                    states.add(((Hair)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(HairHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Hair) {
                    elements.add(element);
                    states.add(((Hair)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(HairHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<Hair> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <Hair> Hair[] toArray(Hair[] array) {
        return set.toArray(array);
    }

    public boolean add(Hair element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends Hair> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Hair)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
