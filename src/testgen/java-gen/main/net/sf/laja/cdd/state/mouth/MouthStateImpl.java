package net.sf.laja.cdd.state.mouth;

import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class MouthStateImpl implements MouthState {
    protected int numberOfTeeth; // (id)
    protected int width; // (key)
    protected int height; // (key)
    protected String color;

    private boolean _encapsulated = false;

    MouthStateImpl() {
    }

    public static MouthStateBuilder build() {
        return new MouthStateBuilderImpl();
    }

    public static MouthStateBuilder build(MouthState state) {
        return new MouthStateBuilderImpl(state);
    }

    public boolean isValid() {
        return isValid(_encapsulated);
    }

    public boolean isValidAsEncapsulated() {
        return isValid(true);
    }

    private boolean isValid(boolean encapsulated) {
        if (color == null) {
            return false;
        }
        return true;
    }

    // Getters
    public int getNumberOfTeeth() { return numberOfTeeth; }
    public int getWidth() { return width; }
    public int getHeight() { return height; }
    public String getColor() { return color; }

    // Setters
    public void setNumberOfTeeth(int numberOfTeeth) { this.numberOfTeeth = numberOfTeeth; }
    public void setWidth(int width) { this.width = width; }
    public void setHeight(int height) { this.height = height; }
    public void setColor(String color) { this.color = color; }

    public void encapsulate() {
        _encapsulated = true;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        MouthStateImpl state = (MouthStateImpl)o;

        if (width != state.getWidth()) return false;
        if (height != state.getHeight()) return false;

        return true;
    }

    public boolean valueEquals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        MouthStateImpl state = (MouthStateImpl)o;

        if (width != state.getWidth()) return false;
        if (height != state.getHeight()) return false;
        if (color != null ? !color.equals(state.color) : state.color != null) return false;

        return true;
    }

    @Override
    public int hashCode() {
        int result = width;
        result = 31 * result + height;

        return result;
    }

    public int valueHashCode() {
        int result = width;
        result = 31 * result + height;
        result = 31 * result + (color != null ? color.hashCode() : 0);

        return result;
    }

    @Override
    public String toString() {
        return "{numberOfTeeth=" + numberOfTeeth +
                ", width=" + width +
                ", height=" + height +
                ", color='" + color + '\'' + "}";
    }
}
