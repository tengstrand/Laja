package net.sf.laja.cdd.state.finger;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class FingerStateSetBuilder {
    private final Certificate certificate;
    private final FingerStateSet states;

    public FingerStateSetBuilder() {
        certificate = Certificate.get(new FingerStateImpl());
        states = FingerStateHashSet.emptySet();
    }

    public FingerStateSetBuilder(FingerStateSet states) {
        this.states = states;
        certificate = Certificate.get(new FingerStateImpl());
    }

    public FingerStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<FingerStateBuilder> getStateBuilders() {
        List<FingerStateBuilder> builders = new ArrayList<FingerStateBuilder>(states.size());

        for (FingerState state : states) {
            builders.add(new FingerStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(FingerStateBuilder builder) {
        states.add(builder.getFingerState(certificate));
    }

    public void add(FingerState state) {
        states.add(state);
    }

    public boolean remove(FingerState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(FingerStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "FingerStateSetBuilder{" + states + "}";
    }
}
