package net.sf.laja.cdd.state.arm;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public final class ArmStateListBuilder {
    private final Certificate certificate;
    private final ArmStateList states;

    public ArmStateListBuilder() {
        certificate = Certificate.get(new ArmStateImpl());
        states = ArmStateListImpl.emptyList();
    }

    public ArmStateListBuilder(ArmStateList states) {
        this.states = states;
        certificate = Certificate.get(new ArmStateImpl());
    }

    public ArmStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<ArmStateBuilder> getStateBuilders() {
        List<ArmStateBuilder> builders = new ArrayList<ArmStateBuilder>(states.size());

        for (ArmState state : states) {
            builders.add(new ArmStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(ArmStateBuilder builder) {
        states.add(builder.getArmState(certificate));
    }

    public void add(ArmState state) {
        states.add(state);
    }

    public void add(int index, ArmState state) {
        states.add(index, state);
    }

    public void addAll(int index, ArmStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, ArmState state) {
        states.set(index, state);
    }

    public boolean remove(ArmState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(ArmStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "ArmStateListBuilder{states=" + states + "}";
    }
}
