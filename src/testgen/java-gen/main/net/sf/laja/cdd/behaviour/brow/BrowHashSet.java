package net.sf.laja.cdd.behaviour.brow;

import net.sf.laja.cdd.state.brow.*;
import net.sf.laja.cdd.behaviour.brow.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class BrowHashSet implements BrowSet, RandomAccess, Cloneable, java.io.Serializable {
    protected BrowStateSet stateSet;
    protected final Set<Brow> set;

    public BrowHashSet(Brow... array) {
        this.set = new HashSet<Brow>();
        this.set.addAll(Arrays.asList(array));
    }

    public BrowHashSet(Collection<Brow> collection) {
        this.set = new HashSet<Brow>();
        this.set.addAll(collection);
    }

    public BrowHashSet(BrowStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<Brow> elements = new HashSet<Brow>(stateSet.size());

        for (BrowState state : stateSet) {
            BrowStateBuilder builder = new BrowStateBuilderImpl(state);
            Brow entry = (Brow) builder.as(new BrowFactory.BrowFactory_());
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public BigBrowHashSet asBigBrowSet() {
        if (stateSet != null) {
            return new BigBrowHashSet(stateSet);
        }
        Set<BigBrow> result = new HashSet<BigBrow>();
        for (Brow entry : set) {
            result.add(entry.asBigBrow());
        }
        return new BigBrowHashSet(result);
    }

    public class StateInSyncSet extends HashSet<Brow> {
        private BrowStateSet stateSet;

        public StateInSyncSet(BrowStateSet stateSet, Set<Brow> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(Brow element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BrowHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends Brow> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BrowHashSet.this);
            boolean modified = super.addAll(collection);

            for (Brow element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BrowHashSet.this);
            if (!(element instanceof Brow)) {
                return false;
            }
            boolean removedState = stateSet.remove(((Brow) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BrowHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Brow) {
                    elements.add(element);
                    states.add(((Brow)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(BrowHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Brow) {
                    elements.add(element);
                    states.add(((Brow)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(BrowHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<Brow> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <Brow> Brow[] toArray(Brow[] array) {
        return set.toArray(array);
    }

    public boolean add(Brow element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends Brow> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Brow)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
