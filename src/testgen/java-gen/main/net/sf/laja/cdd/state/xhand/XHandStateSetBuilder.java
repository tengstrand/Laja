package net.sf.laja.cdd.state.xhand;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class XHandStateSetBuilder {
    private final Certificate certificate;
    private final XHandStateSet states;

    public XHandStateSetBuilder() {
        certificate = Certificate.get(new XHandStateImpl());
        states = XHandStateHashSet.emptySet();
    }

    public XHandStateSetBuilder(XHandStateSet states) {
        this.states = states;
        certificate = Certificate.get(new XHandStateImpl());
    }

    public XHandStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<XHandStateBuilder> getStateBuilders() {
        List<XHandStateBuilder> builders = new ArrayList<XHandStateBuilder>(states.size());

        for (XHandState state : states) {
            builders.add(new XHandStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(XHandStateBuilder builder) {
        states.add(builder.getXHandState(certificate));
    }

    public void add(XHandState state) {
        states.add(state);
    }

    public boolean remove(XHandState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(XHandStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "XHandStateSetBuilder{" + states + "}";
    }
}
