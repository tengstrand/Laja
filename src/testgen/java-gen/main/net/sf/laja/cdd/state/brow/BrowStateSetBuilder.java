package net.sf.laja.cdd.state.brow;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class BrowStateSetBuilder {
    private final Certificate certificate;
    private final BrowStateSet states;

    public BrowStateSetBuilder() {
        certificate = Certificate.get(new BrowStateImpl());
        states = BrowStateHashSet.emptySet();
    }

    public BrowStateSetBuilder(BrowStateSet states) {
        this.states = states;
        certificate = Certificate.get(new BrowStateImpl());
    }

    public BrowStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<BrowStateBuilder> getStateBuilders() {
        List<BrowStateBuilder> builders = new ArrayList<BrowStateBuilder>(states.size());

        for (BrowState state : states) {
            builders.add(new BrowStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(BrowStateBuilder builder) {
        states.add(builder.getBrowState(certificate));
    }

    public void add(BrowState state) {
        states.add(state);
    }

    public boolean remove(BrowState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(BrowStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "BrowStateSetBuilder{" + states + "}";
    }
}
