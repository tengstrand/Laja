package net.sf.laja.cdd.state.forehead;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class ForeheadStateSetBuilder {
    private final Certificate certificate;
    private final ForeheadStateSet states;

    public ForeheadStateSetBuilder() {
        certificate = Certificate.get(new ForeheadStateImpl());
        states = ForeheadStateHashSet.emptySet();
    }

    public ForeheadStateSetBuilder(ForeheadStateSet states) {
        this.states = states;
        certificate = Certificate.get(new ForeheadStateImpl());
    }

    public ForeheadStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<ForeheadStateBuilder> getStateBuilders() {
        List<ForeheadStateBuilder> builders = new ArrayList<ForeheadStateBuilder>(states.size());

        for (ForeheadState state : states) {
            builders.add(new ForeheadStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(ForeheadStateBuilder builder) {
        states.add(builder.getForeheadState(certificate));
    }

    public void add(ForeheadState state) {
        states.add(state);
    }

    public boolean remove(ForeheadState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(ForeheadStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "ForeheadStateSetBuilder{" + states + "}";
    }
}
