package net.sf.laja.cdd.state.nail;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class NailStateListBuilder {
    private final Certificate certificate;
    private final NailStateList states;

    public NailStateListBuilder() {
        certificate = Certificate.get(new NailStateImpl());
        states = NailStateArrayList.emptyList();
    }

    public NailStateListBuilder(NailStateList states) {
        this.states = states;
        certificate = Certificate.get(new NailStateImpl());
    }

    public NailStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<NailStateBuilder> getStateBuilders() {
        List<NailStateBuilder> builders = new ArrayList<NailStateBuilder>(states.size());

        for (NailState state : states) {
            builders.add(new NailStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(NailStateBuilder builder) {
        states.add(builder.getNailState(certificate));
    }

    public void add(NailState state) {
        states.add(state);
    }

    public void add(int index, NailState state) {
        states.add(index, state);
    }

    public void addAll(int index, NailStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, NailState state) {
        states.set(index, state);
    }

    public boolean remove(NailState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(NailStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "NailStateListBuilder{" + states + "}";
    }
}
