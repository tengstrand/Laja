package net.sf.laja.cdd.state.head;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class HeadStateSetBuilder {
    private final Certificate certificate;
    private final HeadStateSet states;

    public HeadStateSetBuilder() {
        certificate = Certificate.get(new HeadStateImpl());
        states = HeadStateHashSet.emptySet();
    }

    public HeadStateSetBuilder(HeadStateSet states) {
        this.states = states;
        certificate = Certificate.get(new HeadStateImpl());
    }

    public HeadStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<HeadStateBuilder> getStateBuilders() {
        List<HeadStateBuilder> builders = new ArrayList<HeadStateBuilder>(states.size());

        for (HeadState state : states) {
            builders.add(new HeadStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(HeadStateBuilder builder) {
        states.add(builder.getHeadState(certificate));
    }

    public void add(HeadState state) {
        states.add(state);
    }

    public boolean remove(HeadState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(HeadStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "HeadStateSetBuilder{" + states + "}";
    }
}
