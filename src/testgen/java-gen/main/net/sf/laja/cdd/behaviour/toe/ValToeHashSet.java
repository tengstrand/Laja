package net.sf.laja.cdd.behaviour.toe;

import net.sf.laja.cdd.state.toe.*;
import net.sf.laja.cdd.behaviour.toe.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class ValToeHashSet implements ValToeSet, RandomAccess, Cloneable, java.io.Serializable {
    protected ToeStateList stateSet;
    protected final Set<ValToe> set;

    public ValToeHashSet(ValToe... array) {
        this.set = new HashSet<ValToe>();
        this.set.addAll(Arrays.asList(array));
    }

    public ValToeHashSet(Collection<ValToe> collection) {
        this.set = new HashSet<ValToe>();
        this.set.addAll(collection);
    }

    public ValToeHashSet(ToeStateList stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<ValToe> elements = new HashSet<ValToe>(stateSet.size());

        for (ToeState state : stateSet) {
            ToeStateBuilder builder = new ToeStateBuilderImpl(state);
            ValToe entry = (ValToe) builder.as(new ToeFactory.ValToeFactory_(builder));
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public class StateInSyncSet extends HashSet<ValToe> {
        private final ToeStateList stateSet;

        public StateInSyncSet(ToeStateList stateSet, Set<ValToe> elements) {
            this.stateSet = stateSet;
            super.addAll(elements);
        }

        @Override
        public boolean add(ValToe element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(ValToeHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends ValToe> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(ValToeHashSet.this);
            boolean modified = super.addAll(collection);

            for (ValToe element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(ValToeHashSet.this);
            if (!(element instanceof ValToe)) {
                return false;
            }
            stateSet.remove(((ValToe) element).getState(stateSet.certificate()));

            return super.remove(element);
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(ValToeHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof ValToe) {
                    elements.add(element);
                    states.add(((ValToe)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(ValToeHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof ValToe) {
                    elements.add(element);
                    states.add(((ValToe)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(ValToeHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<ValToe> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <ValToe> ValToe[] toArray(ValToe[] array) {
        return set.toArray(array);
    }

    public boolean add(ValToe element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends ValToe> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof ValToe)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
