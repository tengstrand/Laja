package net.sf.laja.cdd.state.foot;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class FootStateListImpl extends ArrayList<FootState> implements FootStateList {
    private boolean _encapsulated = false;

    public FootStateListImpl() {
    }

    public FootStateListImpl(int size) {
        super(size);
    }

    public static FootStateList emptyList() {
        return new FootStateListImpl();
    }

    public Certificate certificate() {
        return Certificate.get(this);
    }

    public boolean isValid() {
        for (FootState state : this) {
            if (!state.isValid()) {
                return false;
            }
        }
        return true;
    }

    public void encapsulate() {
        _encapsulated = true;

        for (FootState state : this) {
            state.encapsulate();
        }
    }
}
