package net.sf.laja.cdd.behaviour.foot;

import net.sf.laja.cdd.state.foot.*;
import net.sf.laja.cdd.behaviour.foot.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class FootHashSet implements FootSet, RandomAccess, Cloneable, java.io.Serializable {
    protected FootStateList stateSet;
    protected final Set<Foot> set;

    public FootHashSet(Foot... array) {
        this.set = new HashSet<Foot>();
        this.set.addAll(Arrays.asList(array));
    }

    public FootHashSet(Collection<Foot> collection) {
        this.set = new HashSet<Foot>();
        this.set.addAll(collection);
    }

    public FootHashSet(FootStateList stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<Foot> elements = new HashSet<Foot>(stateSet.size());

        for (FootState state : stateSet) {
            FootStateBuilder builder = new FootStateBuilderImpl(state);
            Foot entry = (Foot) builder.as(new FootFactory.FootFactory_(builder));
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public class StateInSyncSet extends HashSet<Foot> {
        private final FootStateList stateSet;

        public StateInSyncSet(FootStateList stateSet, Set<Foot> elements) {
            this.stateSet = stateSet;
            super.addAll(elements);
        }

        @Override
        public boolean add(Foot element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FootHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends Foot> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FootHashSet.this);
            boolean modified = super.addAll(collection);

            for (Foot element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FootHashSet.this);
            if (!(element instanceof Foot)) {
                return false;
            }
            stateSet.remove(((Foot) element).getState(stateSet.certificate()));

            return super.remove(element);
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FootHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Foot) {
                    elements.add(element);
                    states.add(((Foot)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FootHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Foot) {
                    elements.add(element);
                    states.add(((Foot)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(FootHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<Foot> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <Foot> Foot[] toArray(Foot[] array) {
        return set.toArray(array);
    }

    public boolean add(Foot element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends Foot> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Foot)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
