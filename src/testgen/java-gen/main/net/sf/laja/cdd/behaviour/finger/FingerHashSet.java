package net.sf.laja.cdd.behaviour.finger;

import net.sf.laja.cdd.state.finger.*;
import net.sf.laja.cdd.behaviour.finger.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class FingerHashSet implements FingerSet, RandomAccess, Cloneable, java.io.Serializable {
    protected FingerStateSet stateSet;
    protected final Set<Finger> set;

    public FingerHashSet(Finger... array) {
        this.set = new HashSet<Finger>();
        this.set.addAll(Arrays.asList(array));
    }

    public FingerHashSet(Collection<Finger> collection) {
        this.set = new HashSet<Finger>();
        this.set.addAll(collection);
    }

    public FingerHashSet(FingerStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<Finger> elements = new HashSet<Finger>(stateSet.size());

        for (FingerState state : stateSet) {
            FingerStateBuilder builder = new FingerStateBuilderImpl(state);
            Finger entry = (Finger) builder.as(new FingerFactory.FingerFactory_(builder));
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public class StateInSyncSet extends HashSet<Finger> {
        private FingerStateSet stateSet;

        public StateInSyncSet(FingerStateSet stateSet, Set<Finger> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(Finger element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FingerHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends Finger> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FingerHashSet.this);
            boolean modified = super.addAll(collection);

            for (Finger element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FingerHashSet.this);
            if (!(element instanceof Finger)) {
                return false;
            }
            boolean removedState = stateSet.remove(((Finger) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FingerHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Finger) {
                    elements.add(element);
                    states.add(((Finger)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(FingerHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Finger) {
                    elements.add(element);
                    states.add(((Finger)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(FingerHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<Finger> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <Finger> Finger[] toArray(Finger[] array) {
        return set.toArray(array);
    }

    public boolean add(Finger element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends Finger> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Finger)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
