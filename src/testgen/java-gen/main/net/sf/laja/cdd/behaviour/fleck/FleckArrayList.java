package net.sf.laja.cdd.behaviour.fleck;

import net.sf.laja.cdd.state.fleck.*;
import net.sf.laja.cdd.behaviour.fleck.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class FleckArrayList implements FleckList, RandomAccess, Cloneable, java.io.Serializable {
    protected FleckStateList stateList;
    protected final List<Fleck> list;

    public FleckArrayList(Fleck... list) {
        this.list = new ArrayList<Fleck>();
        this.list.addAll(Arrays.asList(list));
    }

    public FleckArrayList(Collection<Fleck> list) {
        this.list = new ArrayList<Fleck>();
        this.list.addAll(list);
    }

    public FleckArrayList(FleckStateList stateList) {
        this.stateList = stateList;
        this.stateList.encapsulate(this);
        List<Fleck> elements = new ArrayList<Fleck>(stateList.size());

        for (FleckState state : stateList) {
            FleckStateBuilder builder = new FleckStateBuilderImpl(state);
            Fleck entry = (Fleck) builder.as(new FleckFactory.FleckFactory_(builder));
            elements.add(entry);
        }
        this.list = new StateInSyncList(stateList, elements);
    }

    public class StateInSyncList extends ArrayList<Fleck> {
        private final FleckStateList stateList;

        public StateInSyncList(FleckStateList stateList, List<Fleck> elements) {
            this.stateList = stateList;
            super.addAll(elements);
        }

        @Override
        public boolean add(Fleck element) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            stateList.add(element.getState(stateList.certificate()));
            return super.add(element);
        }

        @Override
        public void add(int index, Fleck element) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            stateList.add(index, element.getState(stateList.certificate()));
            super.add(index, element);
        }

        @Override
        public boolean addAll(Collection<? extends Fleck> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            boolean modified = super.addAll(collection);

            for (Fleck element : collection) {
                stateList.add(element.getState(stateList.certificate()));
            }
            return modified;
        }

        @Override
        public boolean addAll(int index, Collection<? extends Fleck> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            boolean modified = super.addAll(index, collection);

            List elements = new ArrayList(collection.size());
            for (Fleck element : collection) {
                elements.add(element.getState(stateList.certificate()));
            }
            stateList.addAll(index, elements);

            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            if (!(element instanceof Fleck)) {
                return false;
            }
            stateList.remove(((Fleck) element).getState(stateList.certificate()));

            return super.remove(element);
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            List states = new ArrayList(collection.size());
            List elements = new ArrayList(collection.size());
            for (Object element : collection) {
                if (element instanceof Fleck) {
                    elements.add(element);
                    states.add(((Fleck)element).getState(stateList.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateList.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            List states = new ArrayList(collection.size());
            List elements = new ArrayList(collection.size());
            for (Object element : collection) {
                if (element instanceof Fleck) {
                    elements.add(element);
                    states.add(((Fleck)element).getState(stateList.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateList.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            stateList.clear();
            super.clear();
        }

        @Override
        public Fleck set(int index, Fleck element) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            stateList.set(index, element.getState(stateList.certificate()));
            return super.set(index, element);
        }

        @Override
        public Fleck remove(int index) {
            stateList.throwExceptionIfNotEncapsulatedBy(FleckArrayList.this);
            stateList.remove(index);
            return super.remove(index);
        }
    }

    public int size() {
        return list.size();
    }

    public boolean isEmpty() {
        return list.isEmpty();
    }

    public boolean contains(Object element) {
        return list.contains(element);
    }

    public Iterator<Fleck> iterator() {
        return list.iterator();
    }

    public Object[] toArray() {
        return list.toArray();
    }

    public <Fleck> Fleck[] toArray(Fleck[] array) {
        return list.toArray(array);
    }

    public boolean add(Fleck element) {
        return list.add(element);
    }

    public void add(int index, Fleck element) {
        list.add(index, element);
    }

    public boolean addAll(Collection<? extends Fleck> collection) {
        return list.addAll(collection);
    }

    public boolean addAll(int index, Collection<? extends Fleck> collection) {
        return list.addAll(index, collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Fleck)) {
            return false;
        }
        return list.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return list.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return list.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return list.retainAll(collection);
    }

    public void clear() {
        list.clear();
    }

    public Fleck get(int index) {
        return list.get(index);
    }

    public Fleck set(int index, Fleck element) {
        return list.set(index, element);
    }

    public Fleck remove(int index) {
        return list.remove(index);
    }

    public int indexOf(Object element) {
        return list.indexOf(element);
    }

    public int lastIndexOf(Object element) {
        return list.lastIndexOf(element);
    }

    public ListIterator<Fleck> listIterator() {
        return list.listIterator();
    }

    public ListIterator<Fleck> listIterator(int index) {
        return list.listIterator(index);
    }

    public List<Fleck> subList(int fromIndex, int toIndex) {
        return list.subList(fromIndex, toIndex);
    }

    @Override
    public boolean equals(Object o) {
        return list.equals(o);
    }

    @Override
    public int hashCode() {
        return list.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + list + '}';
    }
}
