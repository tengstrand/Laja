package net.sf.laja.cdd.state.foot;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class FootStateListBuilder {
    private final Certificate certificate;
    private final FootStateList states;

    public FootStateListBuilder() {
        certificate = Certificate.get(new FootStateImpl());
        states = FootStateArrayList.emptyList();
    }

    public FootStateListBuilder(FootStateList states) {
        this.states = states;
        certificate = Certificate.get(new FootStateImpl());
    }

    public FootStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<FootStateBuilder> getStateBuilders() {
        List<FootStateBuilder> builders = new ArrayList<FootStateBuilder>(states.size());

        for (FootState state : states) {
            builders.add(new FootStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(FootStateBuilder builder) {
        states.add(builder.getFootState(certificate));
    }

    public void add(FootState state) {
        states.add(state);
    }

    public void add(int index, FootState state) {
        states.add(index, state);
    }

    public void addAll(int index, FootStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, FootState state) {
        states.set(index, state);
    }

    public boolean remove(FootState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(FootStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "FootStateListBuilder{" + states + "}";
    }
}
