package net.sf.laja.cdd.state.eye;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class EyeStateSetBuilder {
    private final Certificate certificate;
    private final EyeStateSet states;

    public EyeStateSetBuilder() {
        certificate = Certificate.get(new EyeStateImpl());
        states = EyeStateHashSet.emptySet();
    }

    public EyeStateSetBuilder(EyeStateSet states) {
        this.states = states;
        certificate = Certificate.get(new EyeStateImpl());
    }

    public EyeStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<EyeStateBuilder> getStateBuilders() {
        List<EyeStateBuilder> builders = new ArrayList<EyeStateBuilder>(states.size());

        for (EyeState state : states) {
            builders.add(new EyeStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(EyeStateBuilder builder) {
        states.add(builder.getEyeState(certificate));
    }

    public void add(EyeState state) {
        states.add(state);
    }

    public boolean remove(EyeState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(EyeStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "EyeStateSetBuilder{" + states + "}";
    }
}
