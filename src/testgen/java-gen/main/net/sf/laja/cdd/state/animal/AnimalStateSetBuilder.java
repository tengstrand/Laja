package net.sf.laja.cdd.state.animal;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class AnimalStateSetBuilder {
    private final Certificate certificate;
    private final AnimalStateSet states;

    public AnimalStateSetBuilder() {
        certificate = Certificate.get(new AnimalStateImpl());
        states = AnimalStateHashSet.emptySet();
    }

    public AnimalStateSetBuilder(AnimalStateSet states) {
        this.states = states;
        certificate = Certificate.get(new AnimalStateImpl());
    }

    public AnimalStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<AnimalStateBuilder> getStateBuilders() {
        List<AnimalStateBuilder> builders = new ArrayList<AnimalStateBuilder>(states.size());

        for (AnimalState state : states) {
            builders.add(new AnimalStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(AnimalStateBuilder builder) {
        states.add(builder.getAnimalState(certificate));
    }

    public void add(AnimalState state) {
        states.add(state);
    }

    public boolean remove(AnimalState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(AnimalStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "AnimalStateSetBuilder{" + states + "}";
    }
}
