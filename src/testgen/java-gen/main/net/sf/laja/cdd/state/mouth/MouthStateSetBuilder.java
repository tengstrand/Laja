package net.sf.laja.cdd.state.mouth;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class MouthStateSetBuilder {
    private final Certificate certificate;
    private final MouthStateSet states;

    public MouthStateSetBuilder() {
        certificate = Certificate.get(new MouthStateImpl());
        states = MouthStateHashSet.emptySet();
    }

    public MouthStateSetBuilder(MouthStateSet states) {
        this.states = states;
        certificate = Certificate.get(new MouthStateImpl());
    }

    public MouthStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<MouthStateBuilder> getStateBuilders() {
        List<MouthStateBuilder> builders = new ArrayList<MouthStateBuilder>(states.size());

        for (MouthState state : states) {
            builders.add(new MouthStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(MouthStateBuilder builder) {
        states.add(builder.getMouthState(certificate));
    }

    public void add(MouthState state) {
        states.add(state);
    }

    public boolean remove(MouthState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(MouthStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "MouthStateSetBuilder{" + states + "}";
    }
}
