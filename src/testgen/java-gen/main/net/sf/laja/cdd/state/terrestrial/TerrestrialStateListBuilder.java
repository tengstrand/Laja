package net.sf.laja.cdd.state.terrestrial;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public final class TerrestrialStateListBuilder {
    private Object lastMutator;
    private final Certificate certificate;
    private final TerrestrialStateList states;

    public TerrestrialStateListBuilder() {
        certificate = Certificate.get(new TerrestrialStateImpl());
        states = TerrestrialStateListImpl.emptyList(certificate);
    }

    public TerrestrialStateListBuilder(TerrestrialStateList states) {
        this.states = states;
        certificate = Certificate.get(new TerrestrialStateImpl());
    }

    public TerrestrialStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<TerrestrialStateBuilder> getStateBuilders() {
        List<TerrestrialStateBuilder> builders = new ArrayList<TerrestrialStateBuilder>(states.size());

        for (TerrestrialState state : states) {
            builders.add(new TerrestrialStateBuilderImpl(state));
        }
        return builders;
    }

    public void throwExceptionIfOutOfSync(Object accessor) {
        if (lastMutator != null && lastMutator != accessor) {
            throw new IllegalStateException("The state has been changed by another list. Call syncState() to synchronize the internal state with the enclosing list.");
        }
    }

    private void prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(Object mutator) {
        throwExceptionIfOutOfSync(mutator);
        this.lastMutator = mutator;
    }

    public void syncState() {
        this.lastMutator = null;
    }

    public int size() {
        return states.size();
    }

    public void clear(Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.clear();
    }

    public void add(TerrestrialStateBuilder builder) {
        states.add(builder.getTerrestrialState(certificate));
    }

    public void add(TerrestrialState state) {
        states.add(state);
    }

    public void add(TerrestrialState state, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.add(state);
    }

    public void add(int index, TerrestrialState state, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.add(index, state);
    }

    public void addAll(int index, TerrestrialStateListBuilder listBuilder, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, TerrestrialState state, Object accessor) {
        throwExceptionIfOutOfSync(accessor);
        states.set(index, state);
    }

    public boolean remove(TerrestrialState state, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        return states.remove(state);
    }

    public void remove(int index, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.remove(index);
    }

    public void retainAll(TerrestrialStateListBuilder retainStates, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "TerrestrialStateListBuilder{states=" + states + ", lastMutator.hashCode()=" + (lastMutator == null ? null : lastMutator.hashCode()) + "}";
    }
}
