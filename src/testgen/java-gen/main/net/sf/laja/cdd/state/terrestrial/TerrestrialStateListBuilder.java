package net.sf.laja.cdd.state.terrestrial;

import java.util.ArrayList;
import java.util.List;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class TerrestrialStateListBuilder {
    private final Certificate certificate;
    private final TerrestrialStateList states;

    public TerrestrialStateListBuilder() {
        certificate = Certificate.get(new TerrestrialStateImpl());
        states = TerrestrialStateArrayList.emptyList();
    }

    public TerrestrialStateListBuilder(TerrestrialStateList states) {
        this.states = states;
        certificate = Certificate.get(new TerrestrialStateImpl());
    }

    public TerrestrialStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<TerrestrialStateBuilder> getStateBuilders() {
        List<TerrestrialStateBuilder> builders = new ArrayList<TerrestrialStateBuilder>(states.size());

        for (TerrestrialState state : states) {
            builders.add(new TerrestrialStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(TerrestrialStateBuilder builder) {
        states.add(builder.getTerrestrialState(certificate));
    }

    public void add(TerrestrialState state) {
        states.add(state);
    }

    public void add(int index, TerrestrialState state) {
        states.add(index, state);
    }

    public void addAll(int index, TerrestrialStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, TerrestrialState state) {
        states.set(index, state);
    }

    public boolean remove(TerrestrialState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(TerrestrialStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "TerrestrialStateListBuilder{" + states + "}";
    }
}
