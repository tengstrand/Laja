package net.sf.laja.cdd.state.ear;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class EarStateSetBuilder {
    private final Certificate certificate;
    private final EarStateSet states;

    public EarStateSetBuilder() {
        certificate = Certificate.get(new EarStateImpl());
        states = EarStateHashSet.emptySet();
    }

    public EarStateSetBuilder(EarStateSet states) {
        this.states = states;
        certificate = Certificate.get(new EarStateImpl());
    }

    public EarStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<EarStateBuilder> getStateBuilders() {
        List<EarStateBuilder> builders = new ArrayList<EarStateBuilder>(states.size());

        for (EarState state : states) {
            builders.add(new EarStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(EarStateBuilder builder) {
        states.add(builder.getEarState(certificate));
    }

    public void add(EarState state) {
        states.add(state);
    }

    public boolean remove(EarState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(EarStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "EarStateSetBuilder{" + states + "}";
    }
}
