package net.sf.laja.cdd.behaviour.eye;

import net.sf.laja.cdd.state.eye.*;
import net.sf.laja.cdd.behaviour.eye.*;

import net.sf.laja.cdd.behaviour.scaryeye.ScaryEye;
import net.sf.laja.cdd.behaviour.scaryeye.ScaryEyeSet;
import net.sf.laja.cdd.behaviour.scaryeye.ScaryEyeHashSet;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class EyeHashSet implements EyeSet, RandomAccess, Cloneable, java.io.Serializable {
    protected EyeStateSet stateSet;
    protected final Set<Eye> set;

    public EyeHashSet(Eye... array) {
        this.set = new HashSet<Eye>();
        this.set.addAll(Arrays.asList(array));
    }

    public EyeHashSet(Collection<Eye> collection) {
        this.set = new HashSet<Eye>();
        this.set.addAll(collection);
    }

    public EyeHashSet(EyeStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<Eye> elements = new HashSet<Eye>(stateSet.size());

        for (EyeState state : stateSet) {
            EyeStateBuilder builder = new EyeStateBuilderImpl(state);
            Eye entry = (Eye) builder.as(new EyeFactory.EyeFactory_(builder));
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public ScaryEyeHashSet asScaryEyeSet() {
        Set<ScaryEye> result = new HashSet<ScaryEye>();
        for (Eye entry : set) {
            result.add(entry.asScaryEye());
        }
        return new ScaryEyeHashSet(result);
    }

    public class StateInSyncSet extends HashSet<Eye> {
        private EyeStateSet stateSet;

        public StateInSyncSet(EyeStateSet stateSet, Set<Eye> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(Eye element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(EyeHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends Eye> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(EyeHashSet.this);
            boolean modified = super.addAll(collection);

            for (Eye element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(EyeHashSet.this);
            if (!(element instanceof Eye)) {
                return false;
            }
            boolean removedState = stateSet.remove(((Eye) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(EyeHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Eye) {
                    elements.add(element);
                    states.add(((Eye)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(EyeHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Eye) {
                    elements.add(element);
                    states.add(((Eye)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(EyeHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<Eye> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <Eye> Eye[] toArray(Eye[] array) {
        return set.toArray(array);
    }

    public boolean add(Eye element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends Eye> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Eye)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
