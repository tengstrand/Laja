package net.sf.laja.cdd.state.toe;

import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class ToeValue implements ToeElement {
    protected final ToeState state;
    protected ToeStateBuilder stateBuilder;

    public ToeValue(ToeState state) {
        this.state = state;
    }

    public ToeValue(ToeState state, ToeStateBuilder stateBuilder) {
        this.state = state;
        this.stateBuilder = stateBuilder;
    }

    public boolean contains(ToeStateList stateList) {
        return stateList.contains(state);
    }

    public ToeState getState(Certificate certificate) {
        return state;
    }

    public ToeState getState(ToeStateList stateList) {
        return state;
    }

    public void addToList(ToeStateList stateList) {
        stateList.add(state);
    }

    @Override
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) return false;

        return state.valueEquals(((ToeValue)o).state);
    }

    @Override
    public int hashCode() {
        return state.valueHashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + state;
    }
}
