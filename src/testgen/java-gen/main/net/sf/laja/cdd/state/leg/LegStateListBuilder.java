package net.sf.laja.cdd.state.leg;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class LegStateListBuilder {
    private final Certificate certificate;
    private final LegStateList states;

    public LegStateListBuilder() {
        certificate = Certificate.get(new LegStateImpl());
        states = LegStateArrayList.emptyList();
    }

    public LegStateListBuilder(LegStateList states) {
        this.states = states;
        certificate = Certificate.get(new LegStateImpl());
    }

    public LegStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<LegStateBuilder> getStateBuilders() {
        List<LegStateBuilder> builders = new ArrayList<LegStateBuilder>(states.size());

        for (LegState state : states) {
            builders.add(new LegStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(LegStateBuilder builder) {
        states.add(builder.getLegState(certificate));
    }

    public void add(LegState state) {
        states.add(state);
    }

    public void add(int index, LegState state) {
        states.add(index, state);
    }

    public void addAll(int index, LegStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, LegState state) {
        states.set(index, state);
    }

    public boolean remove(LegState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(LegStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "LegStateListBuilder{" + states + "}";
    }
}
