package net.sf.laja.cdd.state.toe;

import java.util.*;
import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class ToeStateListImpl extends ArrayList<ToeState> implements ToeStateList {
    private boolean _encapsulated = false;

    public ToeStateListImpl() {
    }

    public static ToeStateList emptyList() {
        return new ToeStateListImpl();
    }

    public Certificate certificate() {
        return Certificate.get(this);
    }

    public boolean isValid() {
        for (ToeState state : this) {
            if (!state.isValid()) {
                return false;
            }
        }
        return true;
    }

    public void encapsulate() {
        _encapsulated = true;

        for (ToeState state : this) {
            state.encapsulate();
        }
    }
}
