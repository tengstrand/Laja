package net.sf.laja.cdd.state.forehead;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public final class ForeheadStateListBuilder {
    private Object lastMutator;
    private final Certificate certificate;
    private final ForeheadStateList states;

    public ForeheadStateListBuilder() {
        certificate = Certificate.get(new ForeheadStateImpl());
        states = ForeheadStateListImpl.emptyList(certificate);
    }

    public ForeheadStateListBuilder(ForeheadStateList states) {
        this.states = states;
        certificate = Certificate.get(new ForeheadStateImpl());
    }

    public ForeheadStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<ForeheadStateBuilder> getStateBuilders() {
        List<ForeheadStateBuilder> builders = new ArrayList<ForeheadStateBuilder>(states.size());

        for (ForeheadState state : states) {
            builders.add(new ForeheadStateBuilderImpl(state));
        }
        return builders;
    }

    public void throwExceptionIfOutOfSync(Object accessor) {
        if (lastMutator != null && lastMutator != accessor) {
            throw new IllegalStateException("The state has been changed by another list. Call syncState() to synchronize the internal state with the enclosing list.");
        }
    }

    private void prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(Object mutator) {
        throwExceptionIfOutOfSync(mutator);
        this.lastMutator = mutator;
    }

    public void syncState() {
        this.lastMutator = null;
    }

    public int size() {
        return states.size();
    }

    public void clear(Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.clear();
    }

    public void add(ForeheadStateBuilder builder) {
        states.add(builder.getForeheadState(certificate));
    }

    public void add(ForeheadState state) {
        states.add(state);
    }

    public void add(ForeheadState state, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.add(state);
    }

    public void add(int index, ForeheadState state, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.add(index, state);
    }

    public void addAll(int index, ForeheadStateListBuilder listBuilder, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, ForeheadState state, Object accessor) {
        throwExceptionIfOutOfSync(accessor);
        states.set(index, state);
    }

    public boolean remove(ForeheadState state, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        return states.remove(state);
    }

    public void remove(int index, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.remove(index);
    }

    public void retainAll(ForeheadStateListBuilder retainStates, Object mutator) {
        prepareAddOrRemoveElementAndThrowExceptionIfOutOfSync(mutator);
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "ForeheadStateListBuilder{states=" + states + ", lastMutator.hashCode()=" + (lastMutator == null ? null : lastMutator.hashCode()) + "}";
    }
}
