package net.sf.laja.cdd.state.xhand;

import net.sf.laja.cdd.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class XHandBehaviour implements XHandStateComparable {
    protected final XHandState state;
    protected XHandStateBuilder stateBuilder;

    public XHandBehaviour(XHandState state) {
        this.state = state;
        state.setEncapsulator(this);
    }

    public XHandBehaviour(XHandState state, XHandStateBuilder stateBuilder) {
        this.state = state;
        this.stateBuilder = stateBuilder;
        state.setEncapsulator(this);
    }

    public XHandState getState(Certificate certificate) {
        if (certificate == null) {
            throw new IllegalArgumentException("Certificate can not be null!");
        }
        return state;
    }
    @Override
    public boolean equals(Object obj) {
        if (!(obj instanceof XHandStateComparable)) return false;
        return state.equals(obj);
    }

    @Override
    public int hashCode() {
        return state.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + state;
    }
}
