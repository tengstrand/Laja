package net.sf.laja.cdd.state.fleck;

import net.sf.laja.cdd.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class FleckStateSetBuilder {
    private final Certificate certificate;
    private final FleckStateSet states;

    public FleckStateSetBuilder() {
        certificate = Certificate.get(new FleckStateImpl());
        states = FleckStateHashSet.emptySet();
    }

    public FleckStateSetBuilder(FleckStateSet states) {
        this.states = states;
        certificate = Certificate.get(new FleckStateImpl());
    }

    public FleckStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<FleckStateBuilder> getStateBuilders() {
        List<FleckStateBuilder> builders = new ArrayList<FleckStateBuilder>(states.size());

        for (FleckState state : states) {
            builders.add(new FleckStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(FleckStateBuilder builder) {
        states.add(builder.getFleckState(certificate));
    }

    public void add(FleckState state) {
        states.add(state);
    }

    public boolean remove(FleckState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(FleckStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "FleckStateSetBuilder{" + states + "}";
    }
}
