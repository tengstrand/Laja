package net.sf.laja.example.car.state;

import net.sf.laja.example.car.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class VehicleSizeStateSetBuilder {
    private final Certificate certificate;
    private final VehicleSizeStateSet states;

    public VehicleSizeStateSetBuilder() {
        certificate = Certificate.get(new VehicleSizeStateImpl());
        states = VehicleSizeStateHashSet.emptySet();
    }

    public VehicleSizeStateSetBuilder(VehicleSizeStateSet states) {
        this.states = states;
        certificate = Certificate.get(new VehicleSizeStateImpl());
    }

    public VehicleSizeStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<VehicleSizeStateBuilder> getStateBuilders() {
        List<VehicleSizeStateBuilder> builders = new ArrayList<VehicleSizeStateBuilder>(states.size());

        for (VehicleSizeState state : states) {
            builders.add(new VehicleSizeStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(VehicleSizeStateBuilder builder) {
        states.add(builder.getVehicleSizeState(certificate));
    }

    public void add(VehicleSizeState state) {
        states.add(state);
    }

    public boolean remove(VehicleSizeState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(VehicleSizeStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "VehicleSizeStateSetBuilder{" + states + "}";
    }
}
