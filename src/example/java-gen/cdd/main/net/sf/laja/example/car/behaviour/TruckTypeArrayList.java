package net.sf.laja.example.car.behaviour;

import net.sf.laja.example.car.state.*;
import net.sf.laja.example.car.behaviour.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class TruckTypeArrayList implements TruckTypeList, RandomAccess, Cloneable, java.io.Serializable {
    protected TruckTypeStateList stateList;
    protected final List<TruckType> list;

    public TruckTypeArrayList(TruckType... list) {
        this.list = new ArrayList<TruckType>();
        this.list.addAll(Arrays.asList(list));
    }

    public TruckTypeArrayList(Collection<TruckType> list) {
        this.list = new ArrayList<TruckType>();
        this.list.addAll(list);
    }

    public TruckTypeArrayList(TruckTypeStateList stateList) {
        this.stateList = stateList;
        this.stateList.encapsulate(this);
        List<TruckType> elements = new ArrayList<TruckType>(stateList.size());

        for (TruckTypeState state : stateList) {
            TruckTypeStateBuilder builder = new TruckTypeStateBuilderImpl(state);
            TruckType entry = (TruckType) builder.as(new TruckTypeFactory.TruckTypeFactory_(builder));
            elements.add(entry);
        }
        this.list = new StateInSyncList(stateList, elements);
    }

    public class StateInSyncList extends ArrayList<TruckType> {
        private final TruckTypeStateList stateList;

        public StateInSyncList(TruckTypeStateList stateList, List<TruckType> elements) {
            this.stateList = stateList;
            super.addAll(elements);
        }

        @Override
        public boolean add(TruckType element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            stateList.add(element.getState(stateList));
            return super.add(element);
        }

        @Override
        public void add(int index, TruckType element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            stateList.add(index, element.getState(stateList));
            super.add(index, element);
        }

        @Override
        public boolean addAll(Collection<? extends TruckType> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            boolean modified = super.addAll(collection);

            for (TruckType element : collection) {
                stateList.add(element.getState(stateList));
            }
            return modified;
        }

        @Override
        public boolean addAll(int index, Collection<? extends TruckType> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            boolean modified = super.addAll(index, collection);

            List elements = new ArrayList(collection.size());
            for (TruckType element : collection) {
                elements.add(element.getState(stateList));
            }
            stateList.addAll(index, elements);

            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            if (!(element instanceof TruckType)) {
                return false;
            }
            stateList.remove(((TruckType) element).getState(stateList));

            return super.remove(element);
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            List states = new ArrayList(collection.size());
            List elements = new ArrayList(collection.size());
            for (Object element : collection) {
                if (element instanceof TruckType) {
                    elements.add(element);
                    states.add(((TruckType)element).getState(stateList));
                }
            }
            boolean modified = super.removeAll(elements);
            stateList.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            List states = new ArrayList(collection.size());
            List elements = new ArrayList(collection.size());
            for (Object element : collection) {
                if (element instanceof TruckType) {
                    elements.add(element);
                    states.add(((TruckType)element).getState(stateList));
                }
            }
            boolean modified = super.retainAll(elements);
            stateList.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            stateList.clear();
            super.clear();
        }

        @Override
        public TruckType set(int index, TruckType element) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            stateList.set(index, element.getState(stateList));
            return super.set(index, element);
        }

        @Override
        public TruckType remove(int index) {
            stateList.throwExceptionIfNotEncapsulatedBy(TruckTypeArrayList.this);
            stateList.remove(index);
            return super.remove(index);
        }
    }

    public int size() {
        return list.size();
    }

    public boolean isEmpty() {
        return list.isEmpty();
    }

    public boolean contains(Object element) {
        return list.contains(element);
    }

    public Iterator<TruckType> iterator() {
        return list.iterator();
    }

    public Object[] toArray() {
        return list.toArray();
    }

    public <TruckType> TruckType[] toArray(TruckType[] array) {
        return list.toArray(array);
    }

    public boolean add(TruckType element) {
        return list.add(element);
    }

    public void add(int index, TruckType element) {
        list.add(index, element);
    }

    public boolean addAll(Collection<? extends TruckType> collection) {
        return list.addAll(collection);
    }

    public boolean addAll(int index, Collection<? extends TruckType> collection) {
        return list.addAll(index, collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof TruckType)) {
            return false;
        }
        return list.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return list.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return list.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return list.retainAll(collection);
    }

    public void clear() {
        list.clear();
    }

    public TruckType get(int index) {
        return list.get(index);
    }

    public TruckType set(int index, TruckType element) {
        return list.set(index, element);
    }

    public TruckType remove(int index) {
        return list.remove(index);
    }

    public int indexOf(Object element) {
        return list.indexOf(element);
    }

    public int lastIndexOf(Object element) {
        return list.lastIndexOf(element);
    }

    public ListIterator<TruckType> listIterator() {
        return list.listIterator();
    }

    public ListIterator<TruckType> listIterator(int index) {
        return list.listIterator(index);
    }

    public List<TruckType> subList(int fromIndex, int toIndex) {
        return list.subList(fromIndex, toIndex);
    }

    @Override
    public boolean equals(Object o) {
        return list.equals(o);
    }

    @Override
    public int hashCode() {
        return list.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + list + '}';
    }
}
