package net.sf.laja.example.repository.behaviour.domain;

import net.sf.laja.example.repository.state.*;
import net.sf.laja.example.repository.behaviour.domain.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class AddressHashSet implements AddressSet, RandomAccess, Cloneable, java.io.Serializable {
    protected AddressStateSet stateSet;
    protected final Set<Address> set;

    public AddressHashSet(Address... array) {
        this.set = new HashSet<Address>();
        this.set.addAll(Arrays.asList(array));
    }

    public AddressHashSet(Collection<Address> collection) {
        this.set = new HashSet<Address>();
        this.set.addAll(collection);
    }

    public AddressHashSet(AddressStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<Address> elements = new HashSet<Address>(stateSet.size());

        for (AddressState state : stateSet) {
            AddressStateBuilder builder = new AddressStateBuilderImpl(state);
            Address entry = (Address) builder.as(new AddressFactory.AddressFactory_(builder));
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public class StateInSyncSet extends HashSet<Address> {
        private final AddressStateSet stateSet;

        public StateInSyncSet(AddressStateSet stateSet, Set<Address> elements) {
            this.stateSet = stateSet;
            super.addAll(elements);
        }

        @Override
        public boolean add(Address element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(AddressHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends Address> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(AddressHashSet.this);
            boolean modified = super.addAll(collection);

            for (Address element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(AddressHashSet.this);
            if (!(element instanceof Address)) {
                return false;
            }
            stateSet.remove(((Address) element).getState(stateSet.certificate()));

            return super.remove(element);
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(AddressHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Address) {
                    elements.add(element);
                    states.add(((Address)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(AddressHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof Address) {
                    elements.add(element);
                    states.add(((Address)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(AddressHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<Address> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <Address> Address[] toArray(Address[] array) {
        return set.toArray(array);
    }

    public boolean add(Address element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends Address> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof Address)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
