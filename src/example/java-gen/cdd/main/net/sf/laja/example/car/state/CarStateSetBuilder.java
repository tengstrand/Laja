package net.sf.laja.example.car.state;

import net.sf.laja.example.car.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class CarStateSetBuilder {
    private final Certificate certificate;
    private final CarStateSet states;

    public CarStateSetBuilder() {
        certificate = Certificate.get(new CarStateImpl());
        states = CarStateHashSet.emptySet();
    }

    public CarStateSetBuilder(CarStateSet states) {
        this.states = states;
        certificate = Certificate.get(new CarStateImpl());
    }

    public CarStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<CarStateBuilder> getStateBuilders() {
        List<CarStateBuilder> builders = new ArrayList<CarStateBuilder>(states.size());

        for (CarState state : states) {
            builders.add(new CarStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(CarStateBuilder builder) {
        states.add(builder.getCarState(certificate));
    }

    public void add(CarState state) {
        states.add(state);
    }

    public boolean remove(CarState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(CarStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "CarStateSetBuilder{" + states + "}";
    }
}
