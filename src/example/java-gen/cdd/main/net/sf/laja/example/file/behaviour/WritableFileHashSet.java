package net.sf.laja.example.file.behaviour;

import net.sf.laja.example.file.state.*;
import net.sf.laja.example.file.behaviour.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class WritableFileHashSet implements WritableFileSet, RandomAccess, Cloneable, java.io.Serializable {
    protected FileStateSet stateSet;
    protected final Set<WritableFile> set;

    public WritableFileHashSet(WritableFile... array) {
        this.set = new HashSet<WritableFile>();
        this.set.addAll(Arrays.asList(array));
    }

    public WritableFileHashSet(Collection<WritableFile> collection) {
        this.set = new HashSet<WritableFile>();
        this.set.addAll(collection);
    }

    public class StateInSyncSet extends HashSet<WritableFile> {
        private FileStateSet stateSet;

        public StateInSyncSet(FileStateSet stateSet, Set<WritableFile> elements) {
            this.stateSet = stateSet;
            this.stateSet.clear();
            super.addAll(elements);
        }

        @Override
        public boolean add(WritableFile element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(WritableFileHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends WritableFile> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(WritableFileHashSet.this);
            boolean modified = super.addAll(collection);

            for (WritableFile element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(WritableFileHashSet.this);
            if (!(element instanceof WritableFile)) {
                return false;
            }
            boolean removedState = stateSet.remove(((WritableFile) element).getState(stateSet.certificate()));
            boolean removedElement = super.remove(element);

            if (removedState != removedElement) {
                throw new IllegalStateException("The state and behaviour is out of sync. Please report this bug to the Laja project!");
            }
            return removedElement;
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(WritableFileHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof WritableFile) {
                    elements.add(element);
                    states.add(((WritableFile)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(WritableFileHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof WritableFile) {
                    elements.add(element);
                    states.add(((WritableFile)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(WritableFileHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<WritableFile> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <WritableFile> WritableFile[] toArray(WritableFile[] array) {
        return set.toArray(array);
    }

    public boolean add(WritableFile element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends WritableFile> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof WritableFile)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
