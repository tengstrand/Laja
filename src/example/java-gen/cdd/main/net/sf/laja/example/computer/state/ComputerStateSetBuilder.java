package net.sf.laja.example.computer.state;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class ComputerStateSetBuilder {
    private final Certificate certificate;
    private final ComputerStateSet states;

    public ComputerStateSetBuilder() {
        certificate = Certificate.get(new ComputerStateImpl());
        states = ComputerStateHashSet.emptySet();
    }

    public ComputerStateSetBuilder(ComputerStateSet states) {
        this.states = states;
        certificate = Certificate.get(new ComputerStateImpl());
    }

    public ComputerStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<ComputerStateBuilder> getStateBuilders() {
        List<ComputerStateBuilder> builders = new ArrayList<ComputerStateBuilder>(states.size());

        for (ComputerState state : states) {
            builders.add(new ComputerStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(ComputerStateBuilder builder) {
        states.add(builder.getComputerState(certificate));
    }

    public void add(ComputerState state) {
        states.add(state);
    }

    public boolean remove(ComputerState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(ComputerStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "ComputerStateSetBuilder{" + states + "}";
    }
}
