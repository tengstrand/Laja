package net.sf.laja.example.car.state;

import net.sf.laja.example.car.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class VehicleStateListBuilder {
    private final Certificate certificate;
    private final VehicleStateList states;

    public VehicleStateListBuilder() {
        certificate = Certificate.get(new VehicleStateImpl());
        states = VehicleStateArrayList.emptyList();
    }

    public VehicleStateListBuilder(VehicleStateList states) {
        this.states = states;
        certificate = Certificate.get(new VehicleStateImpl());
    }

    public VehicleStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<VehicleStateBuilder> getStateBuilders() {
        List<VehicleStateBuilder> builders = new ArrayList<VehicleStateBuilder>(states.size());

        for (VehicleState state : states) {
            builders.add(new VehicleStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(VehicleStateBuilder builder) {
        states.add(builder.getVehicleState(certificate));
    }

    public void add(VehicleState state) {
        states.add(state);
    }

    public void add(int index, VehicleState state) {
        states.add(index, state);
    }

    public void addAll(int index, VehicleStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, VehicleState state) {
        states.set(index, state);
    }

    public boolean remove(VehicleState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(VehicleStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "VehicleStateListBuilder{" + states + "}";
    }
}
