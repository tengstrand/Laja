package net.sf.laja.example.computer.state;

import net.sf.laja.example.computer.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class ComputerValue implements ComputerElement {
    protected final ComputerState state;
    protected ComputerStateBuilder stateBuilder;

    public ComputerValue(ComputerState state) {
        this.state = state;
    }

    public ComputerValue(ComputerState state, ComputerStateBuilder stateBuilder) {
        this.state = state;
        this.stateBuilder = stateBuilder;
    }

    public boolean isStateInSync() {
        return true;
    }

    public boolean syncState() {
        return false;
    }

    public boolean contains(ComputerStateList stateList) {
        return stateList.contains(state);
    }

    public ComputerState getState(Certificate certificate) {
        return state;
    }

    public void addToList(ComputerStateList stateList) {
        stateList.add(state);
    }

    @Override
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) return false;

        return state.valueEquals(((ComputerValue)o).state);
    }

    @Override
    public int hashCode() {
        return state.valueHashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + state;
    }
}
