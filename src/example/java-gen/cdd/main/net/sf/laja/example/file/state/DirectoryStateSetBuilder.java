package net.sf.laja.example.file.state;

import net.sf.laja.example.file.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class DirectoryStateSetBuilder {
    private final Certificate certificate;
    private final DirectoryStateSet states;

    public DirectoryStateSetBuilder() {
        certificate = Certificate.get(new DirectoryStateImpl());
        states = DirectoryStateHashSet.emptySet();
    }

    public DirectoryStateSetBuilder(DirectoryStateSet states) {
        this.states = states;
        certificate = Certificate.get(new DirectoryStateImpl());
    }

    public DirectoryStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<DirectoryStateBuilder> getStateBuilders() {
        List<DirectoryStateBuilder> builders = new ArrayList<DirectoryStateBuilder>(states.size());

        for (DirectoryState state : states) {
            builders.add(new DirectoryStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(DirectoryStateBuilder builder) {
        states.add(builder.getDirectoryState(certificate));
    }

    public void add(DirectoryState state) {
        states.add(state);
    }

    public boolean remove(DirectoryState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(DirectoryStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "DirectoryStateSetBuilder{" + states + "}";
    }
}
