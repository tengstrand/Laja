package net.sf.laja.example.car.state;

import net.sf.laja.example.car.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public final class CarStateListBuilder {
    private final Certificate certificate;
    private final CarStateList states;

    public CarStateListBuilder() {
        certificate = Certificate.get(new CarStateImpl());
        states = CarStateListImpl.emptyList();
    }

    public CarStateListBuilder(CarStateList states) {
        this.states = states;
        certificate = Certificate.get(new CarStateImpl());
    }

    public CarStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<CarStateBuilder> getStateBuilders() {
        List<CarStateBuilder> builders = new ArrayList<CarStateBuilder>(states.size());

        for (CarState state : states) {
            builders.add(new CarStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(CarStateBuilder builder) {
        states.add(builder.getCarState(certificate));
    }

    public void add(CarState state) {
        states.add(state);
    }

    public void add(int index, CarState state) {
        states.add(index, state);
    }

    public void addAll(int index, CarStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, CarState state) {
        states.set(index, state);
    }

    public boolean remove(CarState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(CarStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "CarStateListBuilder{" + states + "}";
    }
}
