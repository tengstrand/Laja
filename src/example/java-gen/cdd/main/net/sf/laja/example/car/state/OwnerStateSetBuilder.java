package net.sf.laja.example.car.state;

import net.sf.laja.example.car.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class OwnerStateSetBuilder {
    private final Certificate certificate;
    private final OwnerStateSet states;

    public OwnerStateSetBuilder() {
        certificate = Certificate.get(new OwnerStateImpl());
        states = OwnerStateHashSet.emptySet();
    }

    public OwnerStateSetBuilder(OwnerStateSet states) {
        this.states = states;
        certificate = Certificate.get(new OwnerStateImpl());
    }

    public OwnerStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<OwnerStateBuilder> getStateBuilders() {
        List<OwnerStateBuilder> builders = new ArrayList<OwnerStateBuilder>(states.size());

        for (OwnerState state : states) {
            builders.add(new OwnerStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(OwnerStateBuilder builder) {
        states.add(builder.getOwnerState(certificate));
    }

    public void add(OwnerState state) {
        states.add(state);
    }

    public boolean remove(OwnerState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(OwnerStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "OwnerStateSetBuilder{" + states + "}";
    }
}
