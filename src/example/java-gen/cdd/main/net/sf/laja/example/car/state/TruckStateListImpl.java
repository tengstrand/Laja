package net.sf.laja.example.car.state;

import java.util.*;
import net.sf.laja.example.car.state.Certificate;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public class TruckStateListImpl extends ArrayList<TruckState> implements TruckStateList {
    private boolean _encapsulated = false;

    public TruckStateListImpl() {
    }

    public TruckStateListImpl(int size) {
        super(size);
    }

    public static TruckStateList emptyList() {
        return new TruckStateListImpl();
    }

    public Certificate certificate() {
        return Certificate.get(this);
    }

    public boolean isValid() {
        for (TruckState state : this) {
            if (!state.isValid()) {
                return false;
            }
        }
        return true;
    }

    public void encapsulate() {
        _encapsulated = true;

        for (TruckState state : this) {
            state.encapsulate();
        }
    }
}
