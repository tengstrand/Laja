package net.sf.laja.example.person.state;

import net.sf.laja.example.person.state.Certificate;

import java.util.ArrayList;
import java.util.List;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.sf.net
 */
public final class PersonStateListBuilder {
    private final Certificate certificate;
    private final PersonStateList states;

    public PersonStateListBuilder() {
        certificate = Certificate.get(new PersonStateImpl());
        states = PersonStateListImpl.emptyList();
    }

    public PersonStateListBuilder(PersonStateList states) {
        this.states = states;
        certificate = Certificate.get(new PersonStateImpl());
    }

    public PersonStateList getStateList(Certificate certificate) {
        return states;
    }

    public List<PersonStateBuilder> getStateBuilders() {
        List<PersonStateBuilder> builders = new ArrayList<PersonStateBuilder>(states.size());

        for (PersonState state : states) {
            builders.add(new PersonStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(PersonStateBuilder builder) {
        states.add(builder.getPersonState(certificate));
    }

    public void add(PersonState state) {
        states.add(state);
    }

    public void add(int index, PersonState state) {
        states.add(index, state);
    }

    public void addAll(int index, PersonStateListBuilder listBuilder) {
        states.addAll(index, listBuilder.states);
    }

    public void set(int index, PersonState state) {
        states.set(index, state);
    }

    public boolean remove(PersonState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(PersonStateListBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "PersonStateListBuilder{" + states + "}";
    }
}
