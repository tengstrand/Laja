package net.sf.laja.example.repository.state;

import net.sf.laja.example.repository.state.Certificate;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public final class AddressStateSetBuilder {
    private final Certificate certificate;
    private final AddressStateSet states;

    public AddressStateSetBuilder() {
        certificate = Certificate.get(new AddressStateImpl());
        states = AddressStateHashSet.emptySet();
    }

    public AddressStateSetBuilder(AddressStateSet states) {
        this.states = states;
        certificate = Certificate.get(new AddressStateImpl());
    }

    public AddressStateSet getStateSet(Certificate certificate) {
        return states;
    }

    public List<AddressStateBuilder> getStateBuilders() {
        List<AddressStateBuilder> builders = new ArrayList<AddressStateBuilder>(states.size());

        for (AddressState state : states) {
            builders.add(new AddressStateBuilderImpl(state));
        }
        return builders;
    }

    public int size() {
        return states.size();
    }

    public void clear() {
        states.clear();
    }

    public void add(AddressStateBuilder builder) {
        states.add(builder.getAddressState(certificate));
    }

    public void add(AddressState state) {
        states.add(state);
    }

    public boolean remove(AddressState state) {
        return states.remove(state);
    }

    public void remove(int index) {
        states.remove(index);
    }

    public void retainAll(AddressStateSetBuilder retainStates) {
        states.retainAll(retainStates.states);
    }

    @Override
    public String toString() {
        return "AddressStateSetBuilder{" + states + "}";
    }
}
