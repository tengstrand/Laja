package net.sf.laja.example.account.behaviour;

import net.sf.laja.example.account.state.*;
import net.sf.laja.example.account.behaviour.*;
import java.util.*;

/**
 * Code generated by Laja - Do not change!
 *
 *   http://laja.tengstrand.nu
 */
public class TestAccountHashSet implements TestAccountSet, RandomAccess, Cloneable, java.io.Serializable {
    protected AccountStateSet stateSet;
    protected final Set<TestAccount> set;

    public TestAccountHashSet(TestAccount... array) {
        this.set = new HashSet<TestAccount>();
        this.set.addAll(Arrays.asList(array));
    }

    public TestAccountHashSet(Collection<TestAccount> collection) {
        this.set = new HashSet<TestAccount>();
        this.set.addAll(collection);
    }

    public TestAccountHashSet(AccountStateSet stateSet) {
        this.stateSet = stateSet;
        this.stateSet.encapsulate(this);
        Set<TestAccount> elements = new HashSet<TestAccount>(stateSet.size());

        for (AccountState state : stateSet) {
            AccountStateBuilder builder = new AccountStateBuilderImpl(state);
            TestAccount entry = (TestAccount) builder.as(new TestAccountFactory.TestAccountFactory_(builder));
            elements.add(entry);
        }
        this.set = new StateInSyncSet(stateSet, elements);
    }

    public SourceAccountHashSet asSourceAccountSet() {
        Set<SourceAccount> result = new HashSet<SourceAccount>();
        for (TestAccount entry : set) {
            result.add(entry.asSourceAccount());
        }
        return new SourceAccountHashSet(result);
    }

    public DestinationAccountHashSet asDestinationAccountSet() {
        Set<DestinationAccount> result = new HashSet<DestinationAccount>();
        for (TestAccount entry : set) {
            result.add(entry.asDestinationAccount());
        }
        return new DestinationAccountHashSet(result);
    }

    public class StateInSyncSet extends HashSet<TestAccount> {
        private final AccountStateSet stateSet;

        public StateInSyncSet(AccountStateSet stateSet, Set<TestAccount> elements) {
            this.stateSet = stateSet;
            super.addAll(elements);
        }

        @Override
        public boolean add(TestAccount element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestAccountHashSet.this);
            stateSet.add(element.getState(stateSet.certificate()));
            return super.add(element);
        }

        @Override
        public boolean addAll(Collection<? extends TestAccount> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestAccountHashSet.this);
            boolean modified = super.addAll(collection);

            for (TestAccount element : collection) {
                stateSet.add(element.getState(stateSet.certificate()));
            }
            return modified;
        }

        @Override
        public boolean remove(Object element) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestAccountHashSet.this);
            if (!(element instanceof TestAccount)) {
                return false;
            }
            stateSet.remove(((TestAccount) element).getState(stateSet.certificate()));

            return super.remove(element);
        }

        @Override
        public boolean removeAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestAccountHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof TestAccount) {
                    elements.add(element);
                    states.add(((TestAccount)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.removeAll(elements);
            stateSet.removeAll(states);

            return modified;
        }

        @Override
        public boolean retainAll(Collection<?> collection) {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestAccountHashSet.this);
            Set states = new HashSet(collection.size());
            Set elements = new HashSet(collection.size());
            for (Object element : collection) {
                if (element instanceof TestAccount) {
                    elements.add(element);
                    states.add(((TestAccount)element).getState(stateSet.certificate()));
                }
            }
            boolean modified = super.retainAll(elements);
            stateSet.retainAll(states);

            return modified;
        }

        @Override
        public void clear() {
            stateSet.throwExceptionIfNotEncapsulatedBy(TestAccountHashSet.this);
            stateSet.clear();
            super.clear();
        }
    }

    public int size() {
        return set.size();
    }

    public boolean isEmpty() {
        return set.isEmpty();
    }

    public boolean contains(Object element) {
        return set.contains(element);
    }

    public Iterator<TestAccount> iterator() {
        return set.iterator();
    }

    public Object[] toArray() {
        return set.toArray();
    }

    public <TestAccount> TestAccount[] toArray(TestAccount[] array) {
        return set.toArray(array);
    }

    public boolean add(TestAccount element) {
        return set.add(element);
    }

    public boolean addAll(Collection<? extends TestAccount> collection) {
        return set.addAll(collection);
    }

    public boolean remove(Object element) {
        if (!(element instanceof TestAccount)) {
            return false;
        }
        return set.remove(element);
    }

    public boolean containsAll(Collection<?> collection) {
        return set.containsAll(collection);
    }

    public boolean removeAll(Collection<?> collection) {
        return set.removeAll(collection);
    }

    public boolean retainAll(Collection<?> collection) {
        return set.retainAll(collection);
    }

    public void clear() {
        set.clear();
    }

    @Override
    public boolean equals(Object o) {
        return set.equals(o);
    }

    @Override
    public int hashCode() {
        return set.hashCode();
    }

    @Override
    public String toString() {
        return getClass().getSimpleName() + "{" + set + '}';
    }
}
