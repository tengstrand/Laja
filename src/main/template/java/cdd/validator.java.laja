
#macro validator()
    public static class {validator} extends StateValidator {
        public {validator}(Object rootElement) { super(rootElement); }
        public {validator}(Object rootElement, String parent, ValidationErrors.Builder errors) { super(rootElement, parent, errors); }

        public void validate({classname} state) {
        }

        public void validate({mutableClass} state) {
        }
    }
#end
